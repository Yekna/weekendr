import Map from "@/components/Map";

// TODO: separate Map component and sidebar component so the sidebar doesn't rerender every time the map does
export default function Home() {
  return <Map />;
}
